-- This pattern is just an example for the RC release, and will be modified in future version.

-- ColumnList
SELECT ColumnName from DBC.COLUMNS
Where databasename ='talend'
and tablename = 'testTable';